import { type Icon, IconCards } from "@tabler/icons-react";

export const toolStatus: Record<string, { icon: Icon; text: string }> = {
  createStudySet: {
    icon: IconCards,
    text: "Creating study set",
  },
};
